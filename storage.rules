
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // For future use. Allows any authenticated user to read/write files.
    // In production, you would lock this down further.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
