
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access for all authenticated users.
    // The app's UI logic restricts who can actually perform uploads (privileged users).
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
